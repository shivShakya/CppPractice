#include<iostream>

struct Rectangle {
       int x ,y ;
       int width , height ;
};

bool overlap(const Rectangle& rect1 , const Rectangle& rect 2){

        
         
         if ( rect1.x = rect2.x && rect1.y = rect2.y && (rect1.x + rect1.length ) = (rect2.x + rect2.length))
}